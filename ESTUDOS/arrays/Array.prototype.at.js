const frutas = ['laranja', 'maçã', 'abacaxi','pêra']

/*O método at() recebe um valor inteiro e retorna o item referente ao index dele, permitindo valores positivos ou negativos.*/

/*Quando um valor negativo é passado como argumento a contagem dos elementos é feita de trás para frente.*/

const ultimaFruta = frutas.at(-1)